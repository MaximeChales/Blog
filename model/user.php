<?php 
class user
{
    public function checkLogin($user, $password){
        //todo verifier login et password (select * for user)
     //   SELECT * FROM `user` WHERE `nickname` = $user AND `password` = $password; 
    }
}