public function addpost(){
    $result = false;
      $comment_id = 0;
      if(isset($_POST['id']) && is_numeric($_POST ['id'])){
        $comment_id = $_GET['id'];
      }
      if ($post_id){
          $post = new post();
         $result =  $post->addpost($post_id);
      }
    
    }